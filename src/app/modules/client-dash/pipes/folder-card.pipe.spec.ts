import { FolderCardPipe } from './folder-card.pipe';

describe('FolderCardPipe', () => {
  it('create an instance', () => {
    const pipe = new FolderCardPipe();
    expect(pipe).toBeTruthy();
  });
});
